# 阶段 1：单层有序链表实现 🔗

## 学习目标
- 理解有序链表的插入、查找、删除操作
- 为理解跳表打下基础
- 掌握链表的基本操作

---

## 1. 为什么从单层链表开始？

跳表本质上是**多层链表的组合**。理解单层链表是理解跳表的基础。

### 普通链表 vs 有序链表

```
普通链表（无序）：
1 -> 5 -> 3 -> 2 -> NULL

有序链表（升序）：
1 -> 2 -> 3 -> 5 -> NULL
```

**有序链表的优势**：
- 查找时可以提前终止（遇到更大的值就停止）
- 为跳表的多层索引打基础

---

## 2. 数据结构设计

### 节点结构

```cpp
template<typename K, typename V>
class Node {
public:
    K key;          // 键
    V value;        // 值
    Node* next;     // 指向下一个节点的指针
    
    // 构造函数
    Node(K k, V v) : key(k), value(v), next(nullptr) {}
};
```

### 链表类结构

```cpp
template<typename K, typename V>
class SortedList {
private:
    Node<K, V>* head;      // 头节点
    int element_count;     // 元素个数
    
public:
    SortedList();          // 构造函数
    ~SortedList();         // 析构函数
    
    void insert(K key, V value);    // 插入
    bool search(K key);             // 查找
    void remove(K key);             // 删除
    void display();                 // 显示
    int size();                     // 获取大小
};
```

---

## 3. 核心操作实现

### 3.1 插入操作

**思路**：
1. 从头节点开始遍历
2. 找到第一个 key 大于等于待插入 key 的节点
3. 在该节点前插入新节点

**图解**：

```
插入 key=5 到链表 [1 -> 3 -> 7 -> NULL]

步骤1：遍历找位置
head -> [1] -> [3] -> [7] -> NULL
                ↑
              current (3 < 5 < 7)

步骤2：插入新节点
head -> [1] -> [3] -> [5] -> [7] -> NULL
```

**代码实现**：

```cpp
void insert(K key, V value) {
    Node<K, V>* newNode = new Node<K, V>(key, value);
    
    // 情况1：链表为空，或新节点应该插在头部
    if (head == nullptr || head->key >= key) {
        newNode->next = head;
        head = newNode;
        element_count++;
        cout << "插入成功: key=" << key << ", value=" << value << endl;
        return;
    }
    
    // 情况2：在中间或末尾插入
    Node<K, V>* current = head;
    
    // 找到插入位置的前一个节点
    while (current->next != nullptr && current->next->key < key) {
        current = current->next;
    }
    
    // 检查是否已存在
    if (current->next != nullptr && current->next->key == key) {
        cout << "key=" << key << " 已存在" << endl;
        delete newNode;
        return;
    }
    
    // 插入新节点
    newNode->next = current->next;
    current->next = newNode;
    element_count++;
    
    cout << "插入成功: key=" << key << ", value=" << value << endl;
}
```

### 3.2 查找操作

**思路**：
1. 从头节点开始遍历
2. 如果找到 key 相等的节点，返回 true
3. 如果遇到更大的 key 或到达末尾，返回 false

**图解**：

```
查找 key=5 在链表 [1 -> 3 -> 5 -> 7 -> NULL]

[1] -> [3] -> [5] -> [7] -> NULL
                ↑
              找到了！
```

**代码实现**：

```cpp
bool search(K key) {
    Node<K, V>* current = head;
    
    // 遍历链表
    while (current != nullptr) {
        if (current->key == key) {
            cout << "找到 key=" << key << ", value=" << current->value << endl;
            return true;
        }
        
        // 优化：如果当前key已经大于目标key，可以提前终止
        if (current->key > key) {
            break;
        }
        
        current = current->next;
    }
    
    cout << "未找到 key=" << key << endl;
    return false;
}
```

### 3.3 删除操作

**思路**：
1. 找到要删除节点的前一个节点
2. 修改前一个节点的 next 指针
3. 释放被删除节点的内存

**图解**：

```
删除 key=5 从链表 [1 -> 3 -> 5 -> 7 -> NULL]

步骤1：找到前一个节点
[1] -> [3] -> [5] -> [7] -> NULL
        ↑      ↑
      prev  current

步骤2：修改指针
[1] -> [3] -----> [7] -> NULL
              [5] (待删除)

步骤3：释放内存
[1] -> [3] -> [7] -> NULL
```

**代码实现**：

```cpp
void remove(K key) {
    if (head == nullptr) {
        cout << "链表为空" << endl;
        return;
    }
    
    // 情况1：删除头节点
    if (head->key == key) {
        Node<K, V>* temp = head;
        head = head->next;
        delete temp;
        element_count--;
        cout << "删除成功: key=" << key << endl;
        return;
    }
    
    // 情况2：删除中间或末尾节点
    Node<K, V>* current = head;
    
    // 找到要删除节点的前一个节点
    while (current->next != nullptr && current->next->key < key) {
        current = current->next;
    }
    
    // 检查是否找到
    if (current->next == nullptr || current->next->key != key) {
        cout << "未找到 key=" << key << endl;
        return;
    }
    
    // 删除节点
    Node<K, V>* temp = current->next;
    current->next = current->next->next;
    delete temp;
    element_count--;
    
    cout << "删除成功: key=" << key << endl;
}
```

### 3.4 显示操作

```cpp
void display() {
    cout << "\n===== 链表内容 =====" << endl;
    Node<K, V>* current = head;
    
    if (current == nullptr) {
        cout << "链表为空" << endl;
        return;
    }
    
    while (current != nullptr) {
        cout << "key:" << current->key << ", value:" << current->value;
        if (current->next != nullptr) {
            cout << " -> ";
        }
        current = current->next;
    }
    cout << " -> NULL" << endl;
    cout << "总元素数: " << element_count << endl;
}
```

---

## 4. 完整代码

**文件名**：`stage1_simple_list.cpp`

```cpp
#include <iostream>
using namespace std;

// 节点类
template<typename K, typename V>
class Node {
public:
    K key;
    V value;
    Node* next;
    
    Node(K k, V v) : key(k), value(v), next(nullptr) {}
};

// 有序链表类
template<typename K, typename V>
class SortedList {
private:
    Node<K, V>* head;
    int element_count;
    
public:
    // 构造函数
    SortedList() : head(nullptr), element_count(0) {
        cout << "有序链表已创建" << endl;
    }
    
    // 析构函数
    ~SortedList() {
        Node<K, V>* current = head;
        while (current != nullptr) {
            Node<K, V>* temp = current;
            current = current->next;
            delete temp;
        }
        cout << "有序链表已销毁，内存已释放" << endl;
    }
    
    // 插入元素
    void insert(K key, V value) {
        Node<K, V>* newNode = new Node<K, V>(key, value);
        
        // 情况1：链表为空，或新节点应该插在头部
        if (head == nullptr || head->key >= key) {
            if (head != nullptr && head->key == key) {
                cout << "key=" << key << " 已存在" << endl;
                delete newNode;
                return;
            }
            newNode->next = head;
            head = newNode;
            element_count++;
            cout << "插入成功: key=" << key << ", value=" << value << endl;
            return;
        }
        
        // 情况2：在中间或末尾插入
        Node<K, V>* current = head;
        
        while (current->next != nullptr && current->next->key < key) {
            current = current->next;
        }
        
        // 检查是否已存在
        if (current->next != nullptr && current->next->key == key) {
            cout << "key=" << key << " 已存在" << endl;
            delete newNode;
            return;
        }
        
        // 插入新节点
        newNode->next = current->next;
        current->next = newNode;
        element_count++;
        
        cout << "插入成功: key=" << key << ", value=" << value << endl;
    }
    
    // 查找元素
    bool search(K key) {
        cout << "\n--- 查找 key=" << key << " ---" << endl;
        Node<K, V>* current = head;
        
        while (current != nullptr) {
            if (current->key == key) {
                cout << "✓ 找到 key=" << key << ", value=" << current->value << endl;
                return true;
            }
            
            if (current->key > key) {
                break;
            }
            
            current = current->next;
        }
        
        cout << "✗ 未找到 key=" << key << endl;
        return false;
    }
    
    // 删除元素
    void remove(K key) {
        cout << "\n--- 删除 key=" << key << " ---" << endl;
        
        if (head == nullptr) {
            cout << "链表为空" << endl;
            return;
        }
        
        // 情况1：删除头节点
        if (head->key == key) {
            Node<K, V>* temp = head;
            head = head->next;
            delete temp;
            element_count--;
            cout << "✓ 删除成功: key=" << key << endl;
            return;
        }
        
        // 情况2：删除中间或末尾节点
        Node<K, V>* current = head;
        
        while (current->next != nullptr && current->next->key < key) {
            current = current->next;
        }
        
        if (current->next == nullptr || current->next->key != key) {
            cout << "✗ 未找到 key=" << key << endl;
            return;
        }
        
        Node<K, V>* temp = current->next;
        current->next = current->next->next;
        delete temp;
        element_count--;
        
        cout << "✓ 删除成功: key=" << key << endl;
    }
    
    // 显示链表
    void display() {
        cout << "\n========== 链表内容 ==========" << endl;
        Node<K, V>* current = head;
        
        if (current == nullptr) {
            cout << "链表为空" << endl;
            return;
        }
        
        while (current != nullptr) {
            cout << "[" << current->key << ":" << current->value << "]";
            if (current->next != nullptr) {
                cout << " -> ";
            }
            current = current->next;
        }
        cout << " -> NULL" << endl;
        cout << "总元素数: " << element_count << endl;
        cout << "=============================" << endl;
    }
    
    // 获取大小
    int size() {
        return element_count;
    }
};

// 主函数：演示程序
int main() {
    cout << "========================================" << endl;
    cout << "  阶段1：有序链表实现演示" << endl;
    cout << "========================================\n" << endl;
    
    // 创建一个键为int，值为string的有序链表
    SortedList<int, string> list;
    
    cout << "\n【测试1：插入元素】" << endl;
    cout << "-------------------" << endl;
    list.insert(3, "三");
    list.insert(1, "一");
    list.insert(5, "五");
    list.insert(2, "二");
    list.insert(4, "四");
    
    list.display();
    
    cout << "\n【测试2：插入重复元素】" << endl;
    cout << "-------------------" << endl;
    list.insert(3, "重复的三");  // 应该失败
    
    cout << "\n【测试3：查找元素】" << endl;
    cout << "-------------------" << endl;
    list.search(3);  // 应该找到
    list.search(6);  // 应该找不到
    
    cout << "\n【测试4：删除元素】" << endl;
    cout << "-------------------" << endl;
    list.remove(1);  // 删除头节点
    list.display();
    
    list.remove(5);  // 删除尾节点
    list.display();
    
    list.remove(3);  // 删除中间节点
    list.display();
    
    cout << "\n【测试5：删除不存在的元素】" << endl;
    cout << "-------------------" << endl;
    list.remove(10);  // 应该失败
    
    cout << "\n【测试6：获取链表大小】" << endl;
    cout << "-------------------" << endl;
    cout << "当前链表大小: " << list.size() << endl;
    
    cout << "\n========================================" << endl;
    cout << "  演示结束" << endl;
    cout << "========================================" << endl;
    
    return 0;
}
```

---

## 5. 编译和运行

### 编译

```bash
cd /Users/cjx/Downloads/Skiplist-CPP-master/learning-plan
g++ -std=c++11 -o stage1 stage1_simple_list.cpp
```

### 运行

```bash
./stage1
```

### 预期输出

```
========================================
  阶段1：有序链表实现演示
========================================

有序链表已创建

【测试1：插入元素】
-------------------
插入成功: key=3, value=三
插入成功: key=1, value=一
插入成功: key=5, value=五
插入成功: key=2, value=二
插入成功: key=4, value=四

========== 链表内容 ==========
[1:一] -> [2:二] -> [3:三] -> [4:四] -> [5:五] -> NULL
总元素数: 5
=============================

【测试2：插入重复元素】
-------------------
key=3 已存在

【测试3：查找元素】
-------------------

--- 查找 key=3 ---
✓ 找到 key=3, value=三

--- 查找 key=6 ---
✗ 未找到 key=6

【测试4：删除元素】
-------------------

--- 删除 key=1 ---
✓ 删除成功: key=1

========== 链表内容 ==========
[2:二] -> [3:三] -> [4:四] -> [5:五] -> NULL
总元素数: 4
=============================

--- 删除 key=5 ---
✓ 删除成功: key=5

========== 链表内容 ==========
[2:二] -> [3:三] -> [4:四] -> NULL
总元素数: 3
=============================

--- 删除 key=3 ---
✓ 删除成功: key=3

========== 链表内容 ==========
[2:二] -> [4:四] -> NULL
总元素数: 2
=============================

【测试5：删除不存在的元素】
-------------------

--- 删除 key=10 ---
✗ 未找到 key=10

【测试6：获取链表大小】
-------------------
当前链表大小: 2

========================================
  演示结束
========================================
有序链表已销毁，内存已释放
```

---

## 6. 时间复杂度分析

| 操作 | 时间复杂度 | 说明 |
|------|-----------|------|
| 插入 | O(n) | 需要遍历找到插入位置 |
| 查找 | O(n) | 最坏情况需要遍历整个链表 |
| 删除 | O(n) | 需要遍历找到删除位置 |

**问题**：链表的查找效率很低，如果有 100 万个元素，查找可能需要遍历 100 万次！

**解决方案**：跳表！通过增加多层索引，将查找时间复杂度降低到 O(log n)。

---

## 7. 练习题

### 练习 1：添加更新功能
为链表添加 `update(K key, V value)` 方法，如果 key 存在则更新 value。

### 练习 2：反向遍历
添加一个 `displayReverse()` 方法，反向打印链表（提示：可以使用递归）。

### 练习 3：查找范围
添加一个 `searchRange(K start, K end)` 方法，查找并打印所有在 [start, end] 范围内的元素。

---

## 8. 常见问题

### Q1：为什么要保持链表有序？
A：有序链表可以在查找时提前终止，为跳表的多层索引打基础。

### Q2：内存泄漏怎么办？
A：确保在析构函数中释放所有节点，使用 valgrind 工具检测内存泄漏。

### Q3：如何调试链表？
A：在关键位置添加 cout 语句，或使用 GDB 调试器。

---

## 9. 下一步

恭喜完成阶段 1！你已经掌握了：
- ✅ 有序链表的实现
- ✅ 插入、查找、删除操作
- ✅ 内存管理

**下一阶段预告**：在阶段 2 中，我们将在单层链表的基础上增加一层索引，实现双层跳表，体验跳表加速查询的魔力！

👉 继续学习：`03-阶段2-双层跳表.md`

