# 阶段 0：前置知识准备 📚

## 学习目标
在开始实现跳表之前，我们需要掌握一些必要的 C++ 知识。

---

## 1. C++ 指针基础

### 什么是指针？
指针是一个变量，它存储的是另一个变量的内存地址。

```cpp
int a = 10;        // 普通变量
int* p = &a;       // 指针变量，存储 a 的地址
cout << *p;        // 输出 10，* 是解引用操作符
```

### 指针的基本操作

```cpp
// 1. 声明指针
int* ptr;

// 2. 获取地址
int num = 42;
ptr = &num;        // & 是取地址操作符

// 3. 解引用（访问指针指向的值）
cout << *ptr;      // 输出 42

// 4. 修改指针指向的值
*ptr = 100;
cout << num;       // 输出 100
```

### 空指针
```cpp
int* ptr = nullptr;  // C++11 推荐使用 nullptr
// int* ptr = NULL;  // 旧式写法

if (ptr == nullptr) {
    cout << "指针为空" << endl;
}
```

---

## 2. 动态内存管理

### new 和 delete

```cpp
// 分配单个对象
int* p = new int;        // 分配一个 int
*p = 42;
delete p;                // 释放内存

// 分配数组
int* arr = new int[10];  // 分配 10 个 int
arr[0] = 1;
delete[] arr;            // 释放数组，注意是 delete[]
```

### 为什么需要动态内存？
- 栈内存有限，大数据结构需要堆内存
- 数据结构的大小在编译时未知
- 需要在函数间传递数据

### ⚠️ 常见错误

```cpp
// 错误1：忘记释放内存（内存泄漏）
int* p = new int;
// ... 使用 p
// 忘记 delete p;  ❌

// 错误2：重复释放
int* p = new int;
delete p;
delete p;  // ❌ 重复释放，程序崩溃

// 错误3：释放后继续使用（悬空指针）
int* p = new int;
delete p;
*p = 10;   // ❌ p 已经被释放
```

---

## 3. 链表数据结构

### 什么是链表？
链表是一种线性数据结构，每个节点包含数据和指向下一个节点的指针。

```
[数据|指针] -> [数据|指针] -> [数据|指针] -> NULL
```

### 链表节点定义

```cpp
struct Node {
    int data;           // 数据域
    Node* next;         // 指针域，指向下一个节点
    
    // 构造函数
    Node(int val) : data(val), next(nullptr) {}
};
```

### 链表基本操作

```cpp
// 1. 创建节点
Node* head = new Node(1);
head->next = new Node(2);
head->next->next = new Node(3);

// 2. 遍历链表
Node* current = head;
while (current != nullptr) {
    cout << current->data << " ";
    current = current->next;
}

// 3. 插入节点（在头部插入）
Node* newNode = new Node(0);
newNode->next = head;
head = newNode;

// 4. 删除节点
Node* temp = head;
head = head->next;
delete temp;
```

---

## 4. C++ 模板基础

### 什么是模板？
模板允许我们编写通用代码，可以处理不同的数据类型。

### 函数模板

```cpp
// 不使用模板（需要为每种类型写一个函数）
int max_int(int a, int b) {
    return a > b ? a : b;
}

double max_double(double a, double b) {
    return a > b ? a : b;
}

// 使用模板（一个函数处理所有类型）
template<typename T>
T max_value(T a, T b) {
    return a > b ? a : b;
}

// 使用
int i = max_value(10, 20);           // T 是 int
double d = max_value(3.14, 2.71);    // T 是 double
```

### 类模板

```cpp
// 不使用模板
class IntBox {
    int value;
public:
    IntBox(int v) : value(v) {}
    int get() { return value; }
};

// 使用模板
template<typename T>
class Box {
    T value;
public:
    Box(T v) : value(v) {}
    T get() { return value; }
};

// 使用
Box<int> intBox(42);
Box<string> strBox("Hello");
```

### 跳表中的模板使用

```cpp
// 跳表使用两个模板参数：K（键）和 V（值）
template<typename K, typename V>
class SkipList {
    // K 可以是 int, string 等
    // V 可以是 int, string, 自定义类型等
};

// 使用
SkipList<int, string> skipList;      // 键是 int，值是 string
SkipList<string, int> anotherList;   // 键是 string，值是 int
```

---

## 5. 结构体和类

### 结构体（struct）

```cpp
struct Point {
    int x;
    int y;
    
    // 结构体也可以有函数
    void print() {
        cout << "(" << x << ", " << y << ")" << endl;
    }
};

Point p;
p.x = 10;
p.y = 20;
p.print();
```

### 类（class）

```cpp
class Rectangle {
private:
    int width;
    int height;
    
public:
    // 构造函数
    Rectangle(int w, int h) : width(w), height(h) {}
    
    // 成员函数
    int area() {
        return width * height;
    }
};

Rectangle rect(10, 20);
cout << rect.area();  // 输出 200
```

### struct vs class
- `struct` 默认成员是 public
- `class` 默认成员是 private
- 其他方面完全相同

---

## 6. 构造函数和析构函数

### 构造函数
对象创建时自动调用，用于初始化。

```cpp
class Student {
    string name;
    int age;
    
public:
    // 默认构造函数
    Student() {
        name = "Unknown";
        age = 0;
    }
    
    // 带参数的构造函数
    Student(string n, int a) {
        name = n;
        age = a;
    }
    
    // 初始化列表（推荐）
    Student(string n, int a) : name(n), age(a) {}
};
```

### 析构函数
对象销毁时自动调用，用于清理资源。

```cpp
class Array {
    int* data;
    int size;
    
public:
    // 构造函数：分配内存
    Array(int s) : size(s) {
        data = new int[size];
        cout << "内存已分配" << endl;
    }
    
    // 析构函数：释放内存
    ~Array() {
        delete[] data;
        cout << "内存已释放" << endl;
    }
};

// 使用
{
    Array arr(10);  // 构造函数被调用
    // ... 使用 arr
}  // 离开作用域，析构函数自动被调用
```

---

## 7. 指针数组

### 什么是指针数组？
指针数组是一个数组，其中每个元素都是指针。

```cpp
// 普通数组
int arr[5] = {1, 2, 3, 4, 5};

// 指针数组（数组的每个元素都是指针）
int* ptrArr[5];

// 为每个指针分配内存
for (int i = 0; i < 5; i++) {
    ptrArr[i] = new int(i * 10);
}

// 使用
cout << *ptrArr[0];  // 输出 0
cout << *ptrArr[1];  // 输出 10

// 释放内存
for (int i = 0; i < 5; i++) {
    delete ptrArr[i];
}
```

### 跳表中的指针数组

```cpp
// 跳表节点有一个指针数组，指向不同层的下一个节点
template<typename K, typename V>
class Node {
public:
    Node<K, V>** forward;  // 指针数组
    
    Node(K k, V v, int level) {
        // 分配指针数组
        forward = new Node<K, V>*[level + 1];
        
        // 初始化为 nullptr
        for (int i = 0; i <= level; i++) {
            forward[i] = nullptr;
        }
    }
    
    ~Node() {
        delete[] forward;  // 释放指针数组
    }
};
```

---

## 8. 实践练习

### 练习 1：指针基础
编写程序，使用指针交换两个变量的值。

```cpp
#include <iostream>
using namespace std;

void swap(int* a, int* b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}

int main() {
    int x = 10, y = 20;
    cout << "交换前: x=" << x << ", y=" << y << endl;
    
    swap(&x, &y);
    
    cout << "交换后: x=" << x << ", y=" << y << endl;
    return 0;
}
```

### 练习 2：简单链表
实现一个简单的链表，支持插入和遍历。

```cpp
#include <iostream>
using namespace std;

struct Node {
    int data;
    Node* next;
    Node(int val) : data(val), next(nullptr) {}
};

class LinkedList {
    Node* head;
    
public:
    LinkedList() : head(nullptr) {}
    
    // 在头部插入
    void insert(int val) {
        Node* newNode = new Node(val);
        newNode->next = head;
        head = newNode;
    }
    
    // 遍历打印
    void print() {
        Node* current = head;
        while (current != nullptr) {
            cout << current->data << " -> ";
            current = current->next;
        }
        cout << "NULL" << endl;
    }
    
    // 析构函数：释放所有节点
    ~LinkedList() {
        Node* current = head;
        while (current != nullptr) {
            Node* temp = current;
            current = current->next;
            delete temp;
        }
    }
};

int main() {
    LinkedList list;
    list.insert(3);
    list.insert(2);
    list.insert(1);
    list.print();  // 输出: 1 -> 2 -> 3 -> NULL
    return 0;
}
```

### 练习 3：模板函数
编写一个模板函数，打印任意类型的数组。

```cpp
#include <iostream>
using namespace std;

template<typename T>
void printArray(T arr[], int size) {
    for (int i = 0; i < size; i++) {
        cout << arr[i] << " ";
    }
    cout << endl;
}

int main() {
    int intArr[] = {1, 2, 3, 4, 5};
    double doubleArr[] = {1.1, 2.2, 3.3};
    
    printArray(intArr, 5);
    printArray(doubleArr, 3);
    
    return 0;
}
```

---

## 9. 编译和运行

### 编译单个文件

```bash
# 编译
g++ -o program program.cpp

# 运行
./program
```

### 使用 C++11 标准

```bash
g++ -std=c++11 -o program program.cpp
```

### 常用编译选项

```bash
# -Wall: 显示所有警告
# -g: 包含调试信息
# -O2: 优化级别2
g++ -std=c++11 -Wall -g -o program program.cpp
```

---

## 10. 检查清单 ✅

在进入下一阶段之前，确保你理解了：

- [ ] 指针的声明、取地址、解引用
- [ ] new 和 delete 的使用
- [ ] 链表的基本概念和操作
- [ ] 模板的基本语法
- [ ] 构造函数和析构函数的作用
- [ ] 指针数组的概念
- [ ] 如何编译和运行 C++ 程序

---

## 下一步

完成前置知识学习后，进入 **阶段 1：单层链表实现** 👉 `02-阶段1-单层链表.md`

在阶段 1 中，我们将实现一个简单的有序链表，这是理解跳表的基础！

